---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: backendpolicies.flowc.io
spec:
  group: flowc.io
  names:
    kind: BackendPolicy
    listKind: BackendPolicyList
    plural: backendpolicies
    singular: backendpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetRef.kind
      name: Target Kind
      type: string
    - jsonPath: .spec.targetRef.name
      name: Target Name
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BackendPolicy is the Schema for the backendpolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of BackendPolicy
            properties:
              defaults:
                description: defaults contains backend configurations that can be
                  overridden.
                properties:
                  circuitBreaker:
                    description: circuitBreaker configures circuit breaking.
                    properties:
                      maxConnections:
                        description: maxConnections is the maximum number of connections
                          to the upstream.
                        format: int32
                        type: integer
                      maxPendingRequests:
                        description: maxPendingRequests is the maximum number of pending
                          requests.
                        format: int32
                        type: integer
                      maxRetries:
                        description: maxRetries is the maximum number of concurrent
                          retries.
                        format: int32
                        type: integer
                    type: object
                  healthCheck:
                    description: healthCheck configures active health checking.
                    properties:
                      healthyThreshold:
                        description: healthyThreshold is the number of consecutive
                          successes to mark healthy.
                        format: int32
                        type: integer
                      interval:
                        description: interval is the health check interval (e.g.,
                          "10s").
                        type: string
                      path:
                        description: path is the HTTP path for health checks.
                        type: string
                      timeout:
                        description: timeout is the health check timeout (e.g., "2s").
                        type: string
                      unhealthyThreshold:
                        description: unhealthyThreshold is the number of consecutive
                          failures to mark unhealthy.
                        format: int32
                        type: integer
                    type: object
                  loadBalancing:
                    description: loadBalancing configures the load balancing algorithm.
                    properties:
                      type:
                        description: type is the load balancing algorithm.
                        enum:
                        - round-robin
                        - least-request
                        - random
                        - consistent-hash
                        type: string
                    type: object
                  retry:
                    description: retry configures retry behavior.
                    properties:
                      attempts:
                        description: attempts is the maximum number of retry attempts.
                        format: int32
                        type: integer
                      backoff:
                        description: backoff is the base retry backoff duration (e.g.,
                          "25ms").
                        type: string
                      retryOn:
                        description: retryOn specifies conditions that trigger a retry
                          (e.g., ["5xx", "connect-failure"]).
                        items:
                          type: string
                        type: array
                    type: object
                  timeout:
                    description: timeout configures connection and request timeouts.
                    properties:
                      connectionTimeout:
                        description: connectionTimeout is the timeout for establishing
                          a connection (e.g., "5s").
                        type: string
                      requestTimeout:
                        description: requestTimeout is the timeout for a complete
                          request (e.g., "30s").
                        type: string
                    type: object
                type: object
              overrides:
                description: overrides contains backend configurations that cannot
                  be overridden.
                properties:
                  circuitBreaker:
                    description: circuitBreaker configures circuit breaking.
                    properties:
                      maxConnections:
                        description: maxConnections is the maximum number of connections
                          to the upstream.
                        format: int32
                        type: integer
                      maxPendingRequests:
                        description: maxPendingRequests is the maximum number of pending
                          requests.
                        format: int32
                        type: integer
                      maxRetries:
                        description: maxRetries is the maximum number of concurrent
                          retries.
                        format: int32
                        type: integer
                    type: object
                  healthCheck:
                    description: healthCheck configures active health checking.
                    properties:
                      healthyThreshold:
                        description: healthyThreshold is the number of consecutive
                          successes to mark healthy.
                        format: int32
                        type: integer
                      interval:
                        description: interval is the health check interval (e.g.,
                          "10s").
                        type: string
                      path:
                        description: path is the HTTP path for health checks.
                        type: string
                      timeout:
                        description: timeout is the health check timeout (e.g., "2s").
                        type: string
                      unhealthyThreshold:
                        description: unhealthyThreshold is the number of consecutive
                          failures to mark unhealthy.
                        format: int32
                        type: integer
                    type: object
                  loadBalancing:
                    description: loadBalancing configures the load balancing algorithm.
                    properties:
                      type:
                        description: type is the load balancing algorithm.
                        enum:
                        - round-robin
                        - least-request
                        - random
                        - consistent-hash
                        type: string
                    type: object
                  retry:
                    description: retry configures retry behavior.
                    properties:
                      attempts:
                        description: attempts is the maximum number of retry attempts.
                        format: int32
                        type: integer
                      backoff:
                        description: backoff is the base retry backoff duration (e.g.,
                          "25ms").
                        type: string
                      retryOn:
                        description: retryOn specifies conditions that trigger a retry
                          (e.g., ["5xx", "connect-failure"]).
                        items:
                          type: string
                        type: array
                    type: object
                  timeout:
                    description: timeout configures connection and request timeouts.
                    properties:
                      connectionTimeout:
                        description: connectionTimeout is the timeout for establishing
                          a connection (e.g., "5s").
                        type: string
                      requestTimeout:
                        description: requestTimeout is the timeout for a complete
                          request (e.g., "30s").
                        type: string
                    type: object
                type: object
              targetRef:
                description: targetRef identifies the resource this policy targets
                  (API or Deployment).
                properties:
                  group:
                    default: flowc.io
                    description: group is the API group of the target resource.
                    type: string
                  kind:
                    description: kind is the kind of the target resource (e.g., Gateway,
                      API, Deployment).
                    type: string
                  name:
                    description: name is the name of the target resource.
                    type: string
                  sectionName:
                    description: sectionName targets a sub-section of the resource
                      (e.g., a specific listener).
                    type: string
                required:
                - kind
                - name
                type: object
            required:
            - targetRef
            type: object
          status:
            description: status defines the observed state of BackendPolicy
            properties:
              conditions:
                description: conditions represent the current state of the BackendPolicy.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
