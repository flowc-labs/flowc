---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: gatewaypolicies.flowc.io
spec:
  group: flowc.io
  names:
    kind: GatewayPolicy
    listKind: GatewayPolicyList
    plural: gatewaypolicies
    singular: gatewaypolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetRef.kind
      name: Target Kind
      type: string
    - jsonPath: .spec.targetRef.name
      name: Target Name
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GatewayPolicy is the Schema for the gatewaypolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of GatewayPolicy
            properties:
              defaults:
                description: defaults contains policy configurations that can be overridden
                  by more specific policies.
                properties:
                  allowedHooks:
                    description: |-
                      allowedHooks restricts which before/during/after hook points developers can use.
                      If empty, all hooks are allowed.
                    items:
                      type: string
                    type: array
                  authn:
                    description: authn contains AuthN stage policies (JWT provider
                      registration).
                    properties:
                      jwtProviders:
                        description: jwtProviders registers JWT identity providers
                          available to APIs on this gateway.
                        items:
                          description: JWTProvider defines a JWT identity provider
                            registration.
                          properties:
                            issuer:
                              description: issuer is the JWT issuer URL.
                              type: string
                            jwksCacheDuration:
                              default: 300s
                              description: jwksCacheDuration is the cache duration
                                for JWKS (e.g., "300s").
                              type: string
                            jwksUri:
                              description: jwksUri is the URL for the JWKS endpoint.
                              type: string
                            name:
                              description: name is a unique identifier for this provider
                                (referenced by APIPolicies).
                              type: string
                          required:
                          - issuer
                          - jwksUri
                          - name
                          type: object
                        type: array
                    type: object
                  entry:
                    description: entry contains Entry stage policies (IP filter, request
                      size limits).
                    properties:
                      ipFilter:
                        description: ipFilter configures IP-based access control.
                        properties:
                          action:
                            description: 'action is the filter action: allow or deny.'
                            enum:
                            - allow
                            - deny
                            type: string
                          cidrs:
                            description: cidrs is the list of CIDR ranges to match.
                            items:
                              type: string
                            type: array
                        required:
                        - action
                        - cidrs
                        type: object
                      requestSizeLimit:
                        description: requestSizeLimit sets the maximum request body
                          size.
                        properties:
                          maxBodySize:
                            description: maxBodySize is the maximum request body size
                              (e.g., "5MB", "1GB").
                            type: string
                        required:
                        - maxBodySize
                        type: object
                    type: object
                  observability:
                    description: observability contains Observability policies (access
                      logging, metrics).
                    properties:
                      accessLog:
                        description: accessLog configures access logging.
                        properties:
                          enabled:
                            description: enabled activates access logging.
                            type: boolean
                          format:
                            description: 'format is the log format: json or text.'
                            enum:
                            - json
                            - text
                            type: string
                          path:
                            description: path is the log output path (stdout, stderr,
                              or file path).
                            type: string
                        type: object
                    type: object
                  ratelimit:
                    description: ratelimit contains RateLimit stage policies (IP-based
                      rate limiting).
                    properties:
                      ipRateLimit:
                        description: ipRateLimit configures IP-based rate limiting.
                        properties:
                          burst:
                            description: burst is the burst allowance above the rate
                              limit.
                            format: int32
                            type: integer
                          requests:
                            description: requests is the number of allowed requests
                              per window.
                            format: int32
                            type: integer
                          window:
                            description: window is the rate limit window duration
                              (e.g., "60s", "1m").
                            type: string
                        required:
                        - requests
                        - window
                        type: object
                    type: object
                  requireAuthn:
                    description: requireAuthN enforces that all APIs on the target
                      must have authentication configured.
                    type: boolean
                type: object
              overrides:
                description: overrides contains policy configurations that cannot
                  be overridden by more specific policies.
                properties:
                  allowedHooks:
                    description: |-
                      allowedHooks restricts which before/during/after hook points developers can use.
                      If empty, all hooks are allowed.
                    items:
                      type: string
                    type: array
                  authn:
                    description: authn contains AuthN stage policies (JWT provider
                      registration).
                    properties:
                      jwtProviders:
                        description: jwtProviders registers JWT identity providers
                          available to APIs on this gateway.
                        items:
                          description: JWTProvider defines a JWT identity provider
                            registration.
                          properties:
                            issuer:
                              description: issuer is the JWT issuer URL.
                              type: string
                            jwksCacheDuration:
                              default: 300s
                              description: jwksCacheDuration is the cache duration
                                for JWKS (e.g., "300s").
                              type: string
                            jwksUri:
                              description: jwksUri is the URL for the JWKS endpoint.
                              type: string
                            name:
                              description: name is a unique identifier for this provider
                                (referenced by APIPolicies).
                              type: string
                          required:
                          - issuer
                          - jwksUri
                          - name
                          type: object
                        type: array
                    type: object
                  entry:
                    description: entry contains Entry stage policies (IP filter, request
                      size limits).
                    properties:
                      ipFilter:
                        description: ipFilter configures IP-based access control.
                        properties:
                          action:
                            description: 'action is the filter action: allow or deny.'
                            enum:
                            - allow
                            - deny
                            type: string
                          cidrs:
                            description: cidrs is the list of CIDR ranges to match.
                            items:
                              type: string
                            type: array
                        required:
                        - action
                        - cidrs
                        type: object
                      requestSizeLimit:
                        description: requestSizeLimit sets the maximum request body
                          size.
                        properties:
                          maxBodySize:
                            description: maxBodySize is the maximum request body size
                              (e.g., "5MB", "1GB").
                            type: string
                        required:
                        - maxBodySize
                        type: object
                    type: object
                  observability:
                    description: observability contains Observability policies (access
                      logging, metrics).
                    properties:
                      accessLog:
                        description: accessLog configures access logging.
                        properties:
                          enabled:
                            description: enabled activates access logging.
                            type: boolean
                          format:
                            description: 'format is the log format: json or text.'
                            enum:
                            - json
                            - text
                            type: string
                          path:
                            description: path is the log output path (stdout, stderr,
                              or file path).
                            type: string
                        type: object
                    type: object
                  ratelimit:
                    description: ratelimit contains RateLimit stage policies (IP-based
                      rate limiting).
                    properties:
                      ipRateLimit:
                        description: ipRateLimit configures IP-based rate limiting.
                        properties:
                          burst:
                            description: burst is the burst allowance above the rate
                              limit.
                            format: int32
                            type: integer
                          requests:
                            description: requests is the number of allowed requests
                              per window.
                            format: int32
                            type: integer
                          window:
                            description: window is the rate limit window duration
                              (e.g., "60s", "1m").
                            type: string
                        required:
                        - requests
                        - window
                        type: object
                    type: object
                  requireAuthn:
                    description: requireAuthN enforces that all APIs on the target
                      must have authentication configured.
                    type: boolean
                type: object
              targetRef:
                description: targetRef identifies the resource this policy targets
                  (Gateway, Listener, or VirtualHost).
                properties:
                  group:
                    default: flowc.io
                    description: group is the API group of the target resource.
                    type: string
                  kind:
                    description: kind is the kind of the target resource (e.g., Gateway,
                      API, Deployment).
                    type: string
                  name:
                    description: name is the name of the target resource.
                    type: string
                  sectionName:
                    description: sectionName targets a sub-section of the resource
                      (e.g., a specific listener).
                    type: string
                required:
                - kind
                - name
                type: object
            required:
            - targetRef
            type: object
          status:
            description: status defines the observed state of GatewayPolicy
            properties:
              conditions:
                description: conditions represent the current state of the GatewayPolicy.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
