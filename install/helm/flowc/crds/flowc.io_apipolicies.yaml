---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: apipolicies.flowc.io
spec:
  group: flowc.io
  names:
    kind: APIPolicy
    listKind: APIPolicyList
    plural: apipolicies
    singular: apipolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetRef.kind
      name: Target Kind
      type: string
    - jsonPath: .spec.targetRef.name
      name: Target Name
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: APIPolicy is the Schema for the apipolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of APIPolicy
            properties:
              defaults:
                description: defaults contains policy configurations that can be overridden
                  by more specific policies.
                properties:
                  authn:
                    description: authn contains AuthN stage policies (provider selection,
                      claims extraction).
                    properties:
                      providers:
                        description: providers selects JWT providers registered in
                          a GatewayPolicy.
                        items:
                          description: AuthNProviderRef references a JWT provider
                            registered in a GatewayPolicy.
                          properties:
                            audiences:
                              description: audiences is the list of required JWT audiences.
                              items:
                                type: string
                              type: array
                            claimsToHeaders:
                              description: claimsToHeaders extracts JWT claims into
                                request headers.
                              items:
                                description: ClaimToHeader maps a JWT claim to a request
                                  header.
                                properties:
                                  claim:
                                    description: claim is the JWT claim name.
                                    type: string
                                  header:
                                    description: header is the request header to set.
                                    type: string
                                required:
                                - claim
                                - header
                                type: object
                              type: array
                            ref:
                              description: ref is the name of the JWT provider registered
                                in a GatewayPolicy.
                              type: string
                          required:
                          - ref
                          type: object
                        type: array
                    type: object
                  authz:
                    description: authz contains AuthZ stage policies (RBAC).
                    properties:
                      rbac:
                        description: rbac configures role-based access control.
                        properties:
                          rules:
                            description: rules is the list of RBAC rules.
                            items:
                              description: RBACRule defines a single RBAC rule.
                              properties:
                                match:
                                  description: match defines which requests this rule
                                    applies to.
                                  properties:
                                    methods:
                                      description: methods is the list of HTTP methods
                                        to match.
                                      items:
                                        type: string
                                      type: array
                                    paths:
                                      description: paths is the list of path patterns
                                        to match.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                name:
                                  description: name is a human-readable name for this
                                    rule.
                                  type: string
                                requires:
                                  description: requires defines what is needed to
                                    satisfy this rule.
                                  properties:
                                    headers:
                                      additionalProperties:
                                        type: string
                                      description: headers are required header values
                                        (header name -> required value pattern).
                                      type: object
                                  type: object
                              required:
                              - match
                              - requires
                              type: object
                            type: array
                        required:
                        - rules
                        type: object
                    type: object
                  customFilters:
                    description: customFilters are user-provided filters positioned
                      at hook points.
                    items:
                      description: CustomFilter defines a user-provided filter at
                        a hook point.
                      properties:
                        config:
                          description: config is arbitrary configuration passed to
                            the filter.
                          x-kubernetes-preserve-unknown-fields: true
                        execution:
                          description: 'execution is the backend type: cel, wasm,
                            ext_proc_wasm, ext_proc_plugin.'
                          enum:
                          - cel
                          - wasm
                          - ext_proc_wasm
                          - ext_proc_plugin
                          type: string
                        expressions:
                          description: expressions holds CEL expressions (for execution=cel).
                          properties:
                            onRequestBody:
                              description: onRequestBody is the CEL expression evaluated
                                when request body arrives.
                              type: string
                            onRequestHeaders:
                              description: onRequestHeaders is the CEL expression
                                evaluated when request headers arrive.
                              type: string
                            onResponseBody:
                              description: onResponseBody is the CEL expression evaluated
                                when response body arrives.
                              type: string
                            onResponseHeaders:
                              description: onResponseHeaders is the CEL expression
                                evaluated when response headers arrive.
                              type: string
                          type: object
                        module:
                          description: module is the path to a .wasm or .so file (for
                            wasm/ext_proc_wasm/ext_proc_plugin).
                          type: string
                        name:
                          description: name is a unique name for this custom filter.
                          type: string
                        stage:
                          description: stage is the hook predicate (e.g., "before:authn",
                            "during:authz", "after:ratelimit").
                          type: string
                        weight:
                          default: 100
                          description: weight determines execution order within the
                            same stage+predicate (lower runs first).
                          type: integer
                      required:
                      - execution
                      - name
                      - stage
                      type: object
                    type: array
                  entry:
                    description: entry contains Entry stage policies (CORS).
                    properties:
                      cors:
                        description: cors configures Cross-Origin Resource Sharing.
                        properties:
                          allowCredentials:
                            description: allowCredentials allows credentials in CORS
                              requests.
                            type: boolean
                          allowHeaders:
                            description: allowHeaders is the list of allowed request
                              headers.
                            items:
                              type: string
                            type: array
                          allowMethods:
                            description: allowMethods is the list of allowed HTTP
                              methods.
                            items:
                              type: string
                            type: array
                          allowOrigins:
                            description: allowOrigins is the list of allowed origins.
                            items:
                              type: string
                            type: array
                          exposeHeaders:
                            description: exposeHeaders is the list of headers exposed
                              to the browser.
                            items:
                              type: string
                            type: array
                          maxAge:
                            description: maxAge is the max cache duration for preflight
                              responses in seconds.
                            type: integer
                        required:
                        - allowOrigins
                        type: object
                    type: object
                  ratelimit:
                    description: ratelimit contains RateLimit stage policies (per-user
                      rate limiting).
                    properties:
                      perUser:
                        description: perUser configures per-user rate limiting (requires
                          identity from AuthN stage).
                        properties:
                          burst:
                            description: burst is the burst allowance.
                            format: int32
                            type: integer
                          identityHeader:
                            description: identityHeader is the header containing the
                              user identity (e.g., "X-User-Id").
                            type: string
                          requests:
                            description: requests is the number of allowed requests
                              per window.
                            format: int32
                            type: integer
                          window:
                            description: window is the rate limit window duration
                              (e.g., "60s").
                            type: string
                        required:
                        - identityHeader
                        - requests
                        - window
                        type: object
                    type: object
                  transformation:
                    description: transformation contains Transformation stage policies
                      (header/body rewriting).
                    properties:
                      request:
                        description: request configures request transformations.
                        properties:
                          addHeaders:
                            additionalProperties:
                              type: string
                            description: addHeaders adds request headers (does not
                              overwrite existing).
                            type: object
                          removeHeaders:
                            description: removeHeaders removes request headers by
                              name.
                            items:
                              type: string
                            type: array
                          setHeaders:
                            additionalProperties:
                              type: string
                            description: setHeaders sets or overwrites request headers.
                            type: object
                        type: object
                      response:
                        description: response configures response transformations.
                        properties:
                          removeHeaders:
                            description: removeHeaders removes response headers by
                              name.
                            items:
                              type: string
                            type: array
                          setHeaders:
                            additionalProperties:
                              type: string
                            description: setHeaders sets or overwrites response headers.
                            type: object
                        type: object
                    type: object
                type: object
              overrides:
                description: overrides contains policy configurations that cannot
                  be overridden by more specific policies.
                properties:
                  authn:
                    description: authn contains AuthN stage policies (provider selection,
                      claims extraction).
                    properties:
                      providers:
                        description: providers selects JWT providers registered in
                          a GatewayPolicy.
                        items:
                          description: AuthNProviderRef references a JWT provider
                            registered in a GatewayPolicy.
                          properties:
                            audiences:
                              description: audiences is the list of required JWT audiences.
                              items:
                                type: string
                              type: array
                            claimsToHeaders:
                              description: claimsToHeaders extracts JWT claims into
                                request headers.
                              items:
                                description: ClaimToHeader maps a JWT claim to a request
                                  header.
                                properties:
                                  claim:
                                    description: claim is the JWT claim name.
                                    type: string
                                  header:
                                    description: header is the request header to set.
                                    type: string
                                required:
                                - claim
                                - header
                                type: object
                              type: array
                            ref:
                              description: ref is the name of the JWT provider registered
                                in a GatewayPolicy.
                              type: string
                          required:
                          - ref
                          type: object
                        type: array
                    type: object
                  authz:
                    description: authz contains AuthZ stage policies (RBAC).
                    properties:
                      rbac:
                        description: rbac configures role-based access control.
                        properties:
                          rules:
                            description: rules is the list of RBAC rules.
                            items:
                              description: RBACRule defines a single RBAC rule.
                              properties:
                                match:
                                  description: match defines which requests this rule
                                    applies to.
                                  properties:
                                    methods:
                                      description: methods is the list of HTTP methods
                                        to match.
                                      items:
                                        type: string
                                      type: array
                                    paths:
                                      description: paths is the list of path patterns
                                        to match.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                name:
                                  description: name is a human-readable name for this
                                    rule.
                                  type: string
                                requires:
                                  description: requires defines what is needed to
                                    satisfy this rule.
                                  properties:
                                    headers:
                                      additionalProperties:
                                        type: string
                                      description: headers are required header values
                                        (header name -> required value pattern).
                                      type: object
                                  type: object
                              required:
                              - match
                              - requires
                              type: object
                            type: array
                        required:
                        - rules
                        type: object
                    type: object
                  customFilters:
                    description: customFilters are user-provided filters positioned
                      at hook points.
                    items:
                      description: CustomFilter defines a user-provided filter at
                        a hook point.
                      properties:
                        config:
                          description: config is arbitrary configuration passed to
                            the filter.
                          x-kubernetes-preserve-unknown-fields: true
                        execution:
                          description: 'execution is the backend type: cel, wasm,
                            ext_proc_wasm, ext_proc_plugin.'
                          enum:
                          - cel
                          - wasm
                          - ext_proc_wasm
                          - ext_proc_plugin
                          type: string
                        expressions:
                          description: expressions holds CEL expressions (for execution=cel).
                          properties:
                            onRequestBody:
                              description: onRequestBody is the CEL expression evaluated
                                when request body arrives.
                              type: string
                            onRequestHeaders:
                              description: onRequestHeaders is the CEL expression
                                evaluated when request headers arrive.
                              type: string
                            onResponseBody:
                              description: onResponseBody is the CEL expression evaluated
                                when response body arrives.
                              type: string
                            onResponseHeaders:
                              description: onResponseHeaders is the CEL expression
                                evaluated when response headers arrive.
                              type: string
                          type: object
                        module:
                          description: module is the path to a .wasm or .so file (for
                            wasm/ext_proc_wasm/ext_proc_plugin).
                          type: string
                        name:
                          description: name is a unique name for this custom filter.
                          type: string
                        stage:
                          description: stage is the hook predicate (e.g., "before:authn",
                            "during:authz", "after:ratelimit").
                          type: string
                        weight:
                          default: 100
                          description: weight determines execution order within the
                            same stage+predicate (lower runs first).
                          type: integer
                      required:
                      - execution
                      - name
                      - stage
                      type: object
                    type: array
                  entry:
                    description: entry contains Entry stage policies (CORS).
                    properties:
                      cors:
                        description: cors configures Cross-Origin Resource Sharing.
                        properties:
                          allowCredentials:
                            description: allowCredentials allows credentials in CORS
                              requests.
                            type: boolean
                          allowHeaders:
                            description: allowHeaders is the list of allowed request
                              headers.
                            items:
                              type: string
                            type: array
                          allowMethods:
                            description: allowMethods is the list of allowed HTTP
                              methods.
                            items:
                              type: string
                            type: array
                          allowOrigins:
                            description: allowOrigins is the list of allowed origins.
                            items:
                              type: string
                            type: array
                          exposeHeaders:
                            description: exposeHeaders is the list of headers exposed
                              to the browser.
                            items:
                              type: string
                            type: array
                          maxAge:
                            description: maxAge is the max cache duration for preflight
                              responses in seconds.
                            type: integer
                        required:
                        - allowOrigins
                        type: object
                    type: object
                  ratelimit:
                    description: ratelimit contains RateLimit stage policies (per-user
                      rate limiting).
                    properties:
                      perUser:
                        description: perUser configures per-user rate limiting (requires
                          identity from AuthN stage).
                        properties:
                          burst:
                            description: burst is the burst allowance.
                            format: int32
                            type: integer
                          identityHeader:
                            description: identityHeader is the header containing the
                              user identity (e.g., "X-User-Id").
                            type: string
                          requests:
                            description: requests is the number of allowed requests
                              per window.
                            format: int32
                            type: integer
                          window:
                            description: window is the rate limit window duration
                              (e.g., "60s").
                            type: string
                        required:
                        - identityHeader
                        - requests
                        - window
                        type: object
                    type: object
                  transformation:
                    description: transformation contains Transformation stage policies
                      (header/body rewriting).
                    properties:
                      request:
                        description: request configures request transformations.
                        properties:
                          addHeaders:
                            additionalProperties:
                              type: string
                            description: addHeaders adds request headers (does not
                              overwrite existing).
                            type: object
                          removeHeaders:
                            description: removeHeaders removes request headers by
                              name.
                            items:
                              type: string
                            type: array
                          setHeaders:
                            additionalProperties:
                              type: string
                            description: setHeaders sets or overwrites request headers.
                            type: object
                        type: object
                      response:
                        description: response configures response transformations.
                        properties:
                          removeHeaders:
                            description: removeHeaders removes response headers by
                              name.
                            items:
                              type: string
                            type: array
                          setHeaders:
                            additionalProperties:
                              type: string
                            description: setHeaders sets or overwrites response headers.
                            type: object
                        type: object
                    type: object
                type: object
              targetRef:
                description: targetRef identifies the resource this policy targets
                  (API or Deployment).
                properties:
                  group:
                    default: flowc.io
                    description: group is the API group of the target resource.
                    type: string
                  kind:
                    description: kind is the kind of the target resource (e.g., Gateway,
                      API, Deployment).
                    type: string
                  name:
                    description: name is the name of the target resource.
                    type: string
                  sectionName:
                    description: sectionName targets a sub-section of the resource
                      (e.g., a specific listener).
                    type: string
                required:
                - kind
                - name
                type: object
            required:
            - targetRef
            type: object
          status:
            description: status defines the observed state of APIPolicy
            properties:
              conditions:
                description: conditions represent the current state of the APIPolicy.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
