---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: listeners.flowc.io
spec:
  group: flowc.io
  names:
    kind: Listener
    listKind: ListenerList
    plural: listeners
    singular: listener
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.gatewayRef
      name: Gateway
      type: string
    - jsonPath: .spec.port
      name: Port
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Listener is the Schema for the listeners API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Listener
            properties:
              address:
                default: 0.0.0.0
                description: address is the bind address (default "0.0.0.0").
                type: string
              gatewayRef:
                description: gatewayRef is the name of the parent Gateway resource.
                type: string
              hostnames:
                description: |-
                  hostnames are the hostnames for this listener (SNI matching + virtual host domains).
                  Each hostname may be an exact name or a wildcard (e.g., "*.example.com").
                  If empty, matches all hostnames.
                items:
                  type: string
                type: array
              http2:
                description: http2 enables HTTP/2 on the listener.
                type: boolean
              port:
                description: port is the bind port; must be unique within the referenced
                  gateway.
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              tls:
                description: tls contains optional TLS configuration.
                properties:
                  caPath:
                    description: caPath is the path to the CA certificate for client
                      verification.
                    type: string
                  certPath:
                    description: certPath is the path to the TLS certificate file.
                    type: string
                  cipherSuites:
                    description: cipherSuites is the list of allowed cipher suites.
                    items:
                      type: string
                    type: array
                  keyPath:
                    description: keyPath is the path to the TLS private key file.
                    type: string
                  minVersion:
                    description: minVersion is the minimum TLS version (e.g., "1.2",
                      "1.3").
                    type: string
                  requireClientCert:
                    description: requireClientCert enables mutual TLS (mTLS).
                    type: boolean
                required:
                - certPath
                - keyPath
                type: object
            required:
            - gatewayRef
            - port
            type: object
          status:
            description: status defines the observed state of Listener
            properties:
              conditions:
                description: conditions represent the current state of the Listener.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                description: phase is the current lifecycle phase.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
